version: "3.9"

services:
  api:
    image: node:20
    working_dir: /usr/src/app
    command: ["node", "apps/api/src/server.js"]
    volumes:
      - ./:/usr/src/app
    environment:
      PORT: 5010
    ports:
      - "5010:5010"
    restart: unless-stopped
